# The bundle is generated by scripts/build-embedding-worker.mjs (wired to
# prebuild/predev), never committed -- a checked-in build artifact goes stale
# the moment embedding-worker.ts is edited.
*
!.gitignore
